    - name: Start the container
      docker_container:
        name: webapp
        image: "indrajid/webapp:{{DOCKER_HASH}}"
        state: started
        published_ports:
          - 0.0.0.0:8081:8080
